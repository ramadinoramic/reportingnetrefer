etl:
  batch_size: 500           # rows per DB upsert batch
  archive_processed: true   # move loaded CSVs to drop/processed/ when done

database:
  pool_recycle: 3600

logging:
  level: INFO
  format: "%(asctime)s [%(levelname)s] %(name)s: %(message)s"
